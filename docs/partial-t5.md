# T5 局部刷新实测（2026-09-08）

## 可配置驱动帧数（2026-09-16）

固件现在从OD配置读取局刷帧数，默认仍为100，不必为每个数值重新编译。首次需要烧录包含本扩展的固件；老固件即使保存同一个字符串也不会改变波形。

```sh
# 一体入口：扫描现有设备，按编号选板子，再输入帧数
uv run --locked python scripts/set_partial_frames.py
# 查询 / 设置 / 恢复默认
uv run --locked python scripts/set_partial_frames.py DEVICE_ADDRESS
uv run --locked python scripts/set_partial_frames.py DEVICE_ADDRESS 160
uv run --locked python scripts/set_partial_frames.py DEVICE_ADDRESS --reset
```

- 存储位置：标准`DataExtended`（TLV `0x2C`）的`custom_string_3`，内容为`lt213a.partial_frames=160`。这是本项目的字符串约定，没有新增TLV或占用官方保留字节。
- 支持整数1–255，表示五张局刷LUT中对应驱动阶段的帧数；不改变阶段次序、过渡方向、电压、PLL或全刷波形。数值越大通常耗时越长，画质需实物确认。1–255是寄存器可表达范围，不代表每个值都完成了光学/全温区验证。
- 每次`epd_region()`读取一次已提交配置，并将相同帧数写入五张LUT。修改在下一次局刷生效，无需重启；不会影响已经开始的刷新。掉电保留，清除设置/恢复默认时回到100。
- 缺少扩展包、空值、不相关字符串、非十进制内容、0或超过255均按100处理，不使整个配置失效。
- 脚本保留serial、安全配置和其他字段，写入前以0600权限备份完整配置到`build/partial-frame-backups/`，重连逐字节验证。若`custom_string_3`已有其他内容，则拒绝覆盖；`--reset`同样不清除别人的字段。可用`--key-file PATH`提供原始16字节认证密钥。

### 本次验证

调试器当前连接的板为`OD84F6BD`（DEVICEID[1]=e284f6bd），与此前序列号测试的`OD6935B2`不同。完整Flash先备份到`build/partial-frames/before-flash.bin`，烧录带verify通过。构建121036 B Flash / 16376 B RAM，比0.3.0基线增加136 B Flash，RAM不变。

对同一全屏基准、相同32×15局部窗口执行100和160帧测试：

| 配置 | 显式END到刷新完成通知 |
|---|---:|
| 100帧 | 2.187 s |
| 160帧 | 3.387 s |

160帧配置在SWD复位后仍正确读回并用于刷新。此测量包含BLE、刷新和关电，不是示波器的纯BUSY时长；窗口旧/新像素相同，仅用于隔离时序变化，未据此评价残影。测试后已完整恢复原配置（有效帧数100），屏幕留下黑白测试图。日志`build/partial-frames/hardware.log`，可用`tests/test_partial_frames_hardware.py DEVICE_ADDRESS`复现。

主机回归覆盖持久化、无配置/非法值回退、其他custom字段内容、UTF-8序列号与安全配置保留；真实配置存储接入虚拟GPIO驱动后，逐字节确认1/100/160/255均写入全部五张LUT，深睡开关两种构建均通过。录入工具还验证配置备份及交互等待期间的并发改动检测。现有serial录入工具的回归同时检查不会丢失帧数设置。

## 以下为2026-09-08的波形调试记录

最新状态：用户确认“局部刷白 100 帧，再写目标 100 帧”比单次 160 帧更干净、笔画正常。两步共约 5.77 秒，当前固件为每次 100 帧；两步策略由测试脚本 --white-first 发起，普通客户端仍是一次标准局部刷新。下文保留调试过程。

驱动依据用户提供的 `A-GDEW0213T5-201021/GDEW0213T5_Arduino_P20201021/GDEW0213T5_Arduino_P/` 内 Arduino 示例和 `Ap_29demo.h`，不再沿用 T5D 波形。

- 局部初始化设置电源 `01: 03 02 21 21`、面板 `00: BF 0D`、PLL `30: 3C`、VCOM `82: 08`、间隔 `50: 47`。
- LUT 首阶段为 `[transition, 01, 0E, 00, 00, 01]`；五个 transition 为 `00 00 20 10 00`，其余填零。
- 局部窗口 `90` 的末字节为 `28`；旧/新平面分别写 `10` / `13`，由固件反转标准 OpenDisplay 单色像素。客户端无需改变极性。
- 使用实际矩形宽高计算平面长度，没有照搬示例仅适用于正方形的长度表达式。
- 保留刷新后关电与现有可选深睡设置，没有引入 MCU 全帧缓冲。

## 验证

`tests/test_epd.c` 验证真实 SPI 输出，包括非正方形窗口、完整 LUT、跨调用切换旧/新平面和像素极性；完整主机测试通过。

通过 CMSIS-DAP / probe-rs 烧录并校验。官方 py-opendisplay 默认设置先压缩全刷原价签建立 etag，再用 `PartialState` 自动计算差异，将价格 13.80 改为 13.81；图片预先逆时针旋转 90°。

原生坐标窗口 `(40, 11, 32, 15)`，旧/新平面合计 120 字节，压缩后 46 字节。PIPE 局部协商成功，设备回报刷新完成，etag 更新，用时约 1.15 秒；未回退全屏上传。原始日志为 `build/partial-hardware.log`。

这是协议和 BUSY 完成的实机验证；用户目视反馈为旧的 0 未擦掉、与新的 1 叠加，因此本次光学验证失败。不能把刷新完成通知当作局部刷新已成功。示例建议五次局部更新后全刷；本次只执行一次局部更新，固件尚未实现自动次数限制。

复测命令：

```sh
uv run --locked python tests/test_partial_hardware.py DEVICE BEFORE.png AFTER.png
```

两张图片应为 104×212 原生方向；脚本先建立基准再局部更新。当前构建 95,700 B Flash / 16,336 B RAM，静态 RAM 没有增加。

## 当前诊断状态

用户确认症状：旧 0 未擦除，新 1 叠加。已核对 SDK 逻辑流为旧平面后新平面、示例数字素材也是 0 黑/1 白，示例的两平面均按位取反；当前 SPI 回归覆盖此顺序与极性。尚未定位是窗口内擦除波形、首次局部初始化状态还是其他硬件差异；保留当前屏幕供近照观察，不继续盲目刷写。

## PDF 对照后的单变量实验

用户补充 `/Users/shiroki/Downloads/GDEW0213T5-spec10c5.pdf`、`/Users/shiroki/Downloads/IL0373da46.pdf` 和 `IMG_8115.HEIC`。
照片显示旧 0 保留，更新大致在预期数字区域。

T5 PDF 第 36 页的 DDX 表确认 `0x50=0x47`（DDX0=0）配合旧/新像素取反，会把标准黑→白变化送至 LUTBW、白→黑送至 LUTWB；因此暂不改变极性。
T5 PDF 第 39 页 R90h 明确 `PT_SCAN=1` 扫描窗口内外、`0` 仅扫描窗口内。IL0373 PDF 第 23 页则把 1 写为 outside only，与面板专用规格文字不同；这里采用 T5 专用规格。

当前单变量实验把 R90h 末字节由示例的 `0x28`（PT_SCAN=0）改成 `0x01`（PT_SCAN=1），保留 LUT、PLL、极性及像素矩形。假设：15 行小窗口的扫描周期不足以完成擦白，扫描全高可恢复驱动时间。此为待实测验证的假设，不是仅凭文档已确认的根因。构建及主机回归通过，资源占用不变。

全高扫描实验结果：仍叠加旧 0，约 1.16 秒，无明显改善，已恢复 PT_SCAN=0。离线逐字节对比确认 SDK 编码后的旧/新区域均等于原图裁切的标准 1bpp 数据。下一轮仅延长 LUTBW 的第二阶段 14→50 帧（其他 LUT 保留 14），用于检验擦白时间不足；仍属诊断参数，未验证前不作为成功修复。

LUTBW=50 帧版本的协议/BUSY 测试完成：相同 46 字节窗口耗时 1.87 秒（原为 1.15 秒），说明阶段长度修改确实改变了刷新时长。光学结果待本轮反馈。

用户反馈：50 帧已使旧 0 变淡，但仍有残影，支持擦白阶段驱动不足的判断。继续保持其他参数，试 100 帧。

100 帧版本协议/BUSY 测试完成：46 字节，同一 32×15 窗口，用时 2.88 秒，等待光学结果。日志 `build/partial-hardware-bw100.log`。

用户进一步澄清：新 1 的黑色仅出现在原先 0 已为黑色的交集，白→黑并未完成。这说明前述仅加强擦白的诊断不完整。下一轮统一五张 LUT 对应阶段为 100 帧，覆盖白→黑及黑→白，并保持各表时序一致。

五张 LUT 统一 100 帧后，用户确认完整的新 1 已出现，但变化区域仍有轻微残影。此结果表明原示例的 14 帧在本块实板上不足以完成两种像素转换；100 帧属于当前实测折中值，不代表无残影或适合所有温度。随后使用保存的 PartialState 在重连后直接局部改回 13.80，验证连续及反向更新，不先全刷。

最终反向验证：不经全刷，重连后直接局部 13.81→13.80，45 字节、2.91 秒；用户确认 0 完整、其余文字条码不变，仅轻微残影。当前保留 PARTIAL_DRIVE_FRAMES=100，测试图维持逆时针 90°，最终屏幕为 13.80。局部更新已实测可用，但不宣称无残影、快速全刷或全温区验证通过。参考示例每五次局部后全刷的建议，客户端需安排定期完整刷新。

## 160 帧对照（用户要求）

将五张 LUT 对应阶段统一由 100 改为 160，其余寄存器、窗口、像素及极性不变。构建和完整主机回归通过，Flash 95,700 B / RAM 16,336 B，资源占用不变。重新烧录后先完整刷新 13.80 建立基准，再局部更新为 13.81，避免把此前残影状态带入比较。

160 帧实机协议/BUSY 验证通过：相同 32×15 窗口和 46 字节压缩流，耗时 4.08 秒，比 100 帧的 2.88 秒增加 1.20 秒（约 42%）。当前屏幕为 13.81，待用户对比残影。日志 `build/partial-hardware-both160.log`。

用户目视确认：160 帧明显更干净。保留 PARTIAL_DRIVE_FRAMES=160；不据此声称完全无残影或已验证所有温度。

## 先白后目标：100 + 100 帧

用户要求对比两步局部刷新。固件阶段改回 100 帧，`tests/test_partial_hardware.py --white-first` 在同一 BLE 连接里，先把差异区域按 X 八像素对齐后填白，再更新为目标图；两步都使用标准 OpenDisplay PartialState/PIPE API，并各自等待刷新完成更新 etag，中途无完整刷新。仅在测试开始完整刷新 13.80 建立一致基准；目标为 13.81。此策略由测试客户端发起两个标准局部请求，固件没有隐式增加第二次刷新。

两步协议/BUSY 验证通过：均为 `(40,11,32,15)` 窗口，刷白 33 字节 / 2.88 秒，目标 25 字节 / 2.89 秒；合计 58 字节 / 5.77 秒。比单次 160 帧的 4.08 秒增加 1.69 秒。当前固件为 100 帧、屏幕为 13.81；等待光学对比反馈。日志 `build/partial-hardware-white100-target100.log`。

用户最终反馈：比单次 160 帧更干净，笔画正常。当前保留每次 100 帧与可复现的 --white-first 两步测试选项；没有把客户端试验自动改为固件内部双刷新。

## 先反色后目标：100 + 100 帧

新增互斥测试选项 `--invert-first`：把当前基准图在差异窗口内逐像素反色（反的是旧图，而非目标图），第一次标准局部更新到该中间图，第二次更新到目标图。窗口仍按 X 八像素对齐，其余区域不变。沿用已部署的 100 帧固件，无需再次烧录。先完整刷新同一 13.80 基准，最终目标为 13.81，以便与白→目标策略对照。

反色→目标实机协议/BUSY 验证通过：两个窗口均为 `(40,11,32,15)`，反色 52 字节 / 2.90 秒，目标 46 字节 / 2.91 秒；共 98 字节 / 5.81 秒。先白后目标为 58 字节 / 5.77 秒，因此总时长几乎相同，待用户比较光学效果。日志 `build/partial-hardware-invert100-target100.log`。

用户反馈旧图反色策略更差，并澄清原意是目标图反色。已纠正 `--invert-first` 为先写目标图在窗口内的反色，再写目标图；此前旧图反色测试不能代表用户请求的策略。继续使用相同完整基准、窗口和 100 帧参数对比。

正确的目标反色→目标测试：反色 46 字节 / 2.88 秒，目标 38 字节 / 2.93 秒，共 84 字节 / 5.81 秒。用户反馈仍不如先刷白。因此当前对比结论为先白 100 + 目标 100 最佳；保留 --invert-first 作为可复现实验（语义已明确为目标反色），恢复先白后目标的显示。日志 `build/partial-hardware-targetinverse100-target100.log`。
